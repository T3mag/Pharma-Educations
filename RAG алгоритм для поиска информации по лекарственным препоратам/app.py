"""Главная точка входа CLI для RAG-системы лекарственного справочника.

Файл нужен как тонкий запускающий слой:
1. Пользователь запускает команды через app.py.
2. Основная логика пока остается в test.py для обратной совместимости.
3. В дальнейшем бизнес-логику можно будет спокойно переносить по модулям,
   не меняя пользовательскую команду запуска.
"""

from __future__ import annotations


# main импортируется из текущего CLI-скрипта, где уже реализованы все режимы.
# Относительный импорт нужен для пакетного режима, абсолютный - для прямого запуска файла.
try:
    from .test import main
except ImportError:
    from test import main


if __name__ == "__main__":
    # main запускает CLI-приложение и обрабатывает аргументы командной строки.
    main()
