import UIKit

enum MinigameFonts {
    
    enum TitleCell {
        static let titleFont = UIFont.systemFont(ofSize: 35, weight: .bold)
        static let subtitleFont: UIFont = .systemFont(ofSize: 13, weight: .semibold)
    }
    
    enum MiniGameCell {
        static let titleFont = UIFont.systemFont(ofSize: 15, weight: .bold)
        static let subtitleFont = UIFont.systemFont(ofSize: 10, weight: .regular)
    }
}

