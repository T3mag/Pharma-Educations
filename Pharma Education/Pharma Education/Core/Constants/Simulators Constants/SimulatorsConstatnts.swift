import UIKit

enum SimulatorsConstatnts {
    enum General {
        static let indentsFromSaveArea: CGFloat = 20
    }
    
    enum GameCell {
        static let indentsFromContentView: CGFloat = 20
        static let heightImageView: CGFloat = 100
        static let buttonSize: CGFloat = 40
    }
    
    enum TitleCell {
        static let topStackViewSpacing: CGFloat = 10
        static let indentsFromSafeArea: CGFloat = 20
    }
}
